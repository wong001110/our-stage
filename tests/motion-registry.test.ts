import{describe,expect,it}from'vitest';import{parseVmd}from'@our-stage/motion-registry';function fixedText(text:string,size:number){const bytes=new TextEncoder().encode(text),out=new Uint8Array(size);out.set(bytes.slice(0,size));return out}function u32(value:number){const bytes=new Uint8Array(4);new DataView(bytes.buffer).setUint32(0,value,true);return bytes}function f32(value:number){const bytes=new Uint8Array(4);new DataView(bytes.buffer).setFloat32(0,value,true);return bytes}function bone(name:string,frame:number,x:number){return[fixedText(name,15),u32(frame),f32(x),f32(0),f32(0),f32(0),f32(0),f32(0),f32(1),new Uint8Array(64)]}function syntheticVmd(changing:boolean){const bones=[...bone('center',0,0),...bone('center',30,changing?1:0)],parts=[fixedText('Vocaloid Motion Data 0002',30),fixedText('test',20),u32(2),...bones,u32(0),u32(0),u32(0),u32(0),u32(0)],size=parts.reduce((sum,item)=>sum+item.length,0),out=new Uint8Array(size);let offset=0;for(const part of parts){out.set(part,offset);offset+=part.length}return out}describe('VMD parser',()=>{it('distinguishes real motion from duplicate pose keys',()=>{expect(parseVmd(syntheticVmd(true)).animatedBoneNames).toEqual(['center']);expect(parseVmd(syntheticVmd(false)).animatedBoneNames).toEqual([])})});
