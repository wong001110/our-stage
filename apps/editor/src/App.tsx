import { useState } from 'react';
import { Download, FilePlus2, Film, FolderOpen, Library, Music2, Save, Sparkles } from 'lucide-react';
import { AiDirectorPanel } from './components/AiDirectorPanel';import { ExportPanel } from './components/ExportPanel';import { MmdViewport } from './components/MmdViewport';import { TimelineEditor } from './components/TimelineEditor';import { ValidationPanel } from './components/ValidationPanel';import { useProjectStore } from './store/projectStore';
export function App(){const[exportOpen,setExportOpen]=useState(false),[aiOpen,setAiOpen]=useState(false),project=useProjectStore(state=>state.project),dirty=useProjectStore(state=>state.dirty),selectedAssetId=useProjectStore(state=>state.selectedAssetId),newProject=useProjectStore(state=>state.newProject),openProject=useProjectStore(state=>state.openProject),saveProject=useProjectStore(state=>state.saveProject),importMotion=useProjectStore(state=>state.importMotion),importAudio=useProjectStore(state=>state.importAudio),selectAsset=useProjectStore(state=>state.selectAsset),selectedAsset=project.assets.find(asset=>asset.assetId===selectedAssetId);return <main className="editor-shell"><header className="topbar"><div className="brand-mark">OS</div><div><h1>Our Stage</h1><p>{project.metadata.name}{dirty?' · Unsaved':''}</p></div><nav className="top-actions"><button type="button" onClick={()=>newProject()}><FilePlus2 size={16}/> New</button><button type="button" onClick={()=>void openProject()}><FolderOpen size={16}/> Open</button><button type="button" onClick={()=>void saveProject()}><Save size={16}/> Save</button><button type="button" onClick={()=>setExportOpen(true)}><Download size={16}/> Export</button><button type="button" className="accent" onClick={()=>setAiOpen(true)}><Sparkles size={16}/> AI Director</button></nav></header><div className="editor-grid"><aside className="asset-panel panel"><div className="panel-heading"><Library size={17}/><strong>Assets</strong></div><div className="asset-import-actions"><button type="button" onClick={()=>void importMotion()}><Film size={14}/> Motion</button><button type="button" onClick={()=>void importAudio()}><Music2 size={14}/> Audio</button></div><div className="asset-list">{project.assets.length===0?<div className="asset-empty"><Film size={24}/><span>Imported models, motions, stages and audio appear here.</span></div>:project.assets.map(asset=><button type="button" key={asset.assetId} className={asset.assetId===selectedAssetId?'asset-row selected':'asset-row'} onClick={()=>selectAsset(asset.assetId)}><span>{asset.title}</span><small>{asset.type}</small></button>)}</div></aside><MmdViewport/><aside className="inspector-panel panel"><div className="panel-heading"><strong>Inspector</strong></div><div className="property-group"><label>Project</label><span>{project.metadata.name}</span></div><div className="property-group"><label>Selected asset</label><span>{selectedAsset?.title??'No asset selected'}</span></div><div className="property-group"><label>Licence</label><span>{selectedAsset?.source?.licence??'Not recorded'}</span></div><div className="property-group"><label>Render preset</label><span>{project.render.presetId}</span></div><div className="property-group"><label>Output</label><span>{project.output.width} × {project.output.height} · {project.output.fps} FPS</span></div><ValidationPanel/></aside></div><TimelineEditor/>{exportOpen&&<ExportPanel onClose={()=>setExportOpen(false)}/>} {aiOpen&&<AiDirectorPanel onClose={()=>setAiOpen(false)}/>}</main>}
